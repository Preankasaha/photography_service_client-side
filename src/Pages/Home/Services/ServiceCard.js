import React from 'react';
import { useLoaderData } from 'react-router-dom';

const ServiceCard = () => {
    const { } = useLoaderData();
    return (
        <div>

        </div>
    );
};

export default ServiceCard;