import React from 'react';
import useTitle from '../../../hooks/useTitle';

const AddService = () => {
    useTitle('AddService')
    return (
        <div>
            add
        </div>
    );
};

export default AddService;